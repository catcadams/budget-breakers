const CreateButton = ({ onClick }) => {
    return (
      <button 
        type="button" 
        onClick={onClick}
      >
        Create
      </button>
    );
  };
  
  export default CreateButton;